[project]
name = "api"
dynamic = ["version"]
requires-python = ">=3.13,<3.14"
dependencies = [
    "protobuf>=6.33.6",
]

[tool.setuptools.packages.find]
where = ["."]

[dependency-groups]
dev = [
    "grpcio-tools>=1.80.0",
]
