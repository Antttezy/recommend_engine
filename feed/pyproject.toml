[project]
name = "feed"
dynamic = ["version"]
requires-python = ">=3.13,<3.14"
dependencies = [
    "core"
]

[tool.uv.sources]
core = {workspace = true}

[tool.setuptools.packages.find]
where = ["."]
